# ===========================================
# Seats Aero Clone - Environment Configuration
# ===========================================

# Scraper Settings
SCRAPE_DELAY_MIN=2
SCRAPE_DELAY_MAX=10
CONCURRENT_SCRAPERS=3
HEADLESS_MODE=true
REQUEST_TIMEOUT=30

# Proxy Configuration
PROXY_ENABLED=false
PROXY_LIST_URL=
PROXY_ROTATION_STRATEGY=per_request  # per_request, per_domain, on_error

# Browser Settings
CHROME_DRIVER_PATH=
BROWSER_TIMEOUT=30

# API Settings
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# Data Settings
DATA_EXPIRY_HOURS=6
MAX_RESULTS_PER_SEARCH=500
